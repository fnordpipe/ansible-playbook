{% for host in groups['all'] %}
object Host "{{ host }}" {
{% if host in groups['dom0'] %}
  import "dom0-host"
{% endif %}
{% if host in groups['domU'] %}
  import "domU-host"
{% endif %}
{% if host in groups['lxc'] %}
  import "lxc-host"
{% endif %}

  address = "{{ host }}"
}
{% endfor %}
